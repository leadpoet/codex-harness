# Harness bakeoff package

See the repository [README](../../README.md) for setup and live-run commands.

The evaluator supports a blind primary review, an independent second review,
and a two-finalist tie-break on three unused ICPs. Run `python -m
experiments.harness_bakeoff.evaluate --help` for the available commands.
