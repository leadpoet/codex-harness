# Codex sourcing harness package

See the repository [README](../../README.md) for setup and live-run commands.

The package supplies the shared provider boundary, limits, and one Codex SDK
adapter. The public entrypoint is `harness.run_icp`.
